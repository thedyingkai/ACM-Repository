#include <bits/stdc++.h>
using namespace std;

// 快速模拟一轮操作：对数组 a, b（均为 vector<int>，长度 n）进行一轮操作，返回本轮总共减少的 a 的和（delta）。
// 操作步骤：
// 1. 对每个 i: t = min(a[i], b[i]); a[i] -= t, b[i] -= t.
// 2. 对每个 i: c[i] = a[(i-1+n)%n].
// 3. 令 a = c.



int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T;
    cin >> T;
    while(T--){

    }
    return 0;
}
